## Search conventions

- NEVER use `grep`, `find`, `ls -R`, or `cat | grep` in the shell.
- Use the `grep` and `glob` tools for searching — they run ripgrep and respect .gitignore.
- If you must use the shell for search, use `rg` (ripgrep) or `fd`.

