# change columns
set ratios 1:1

cmd quit-to-cwd ${{
  echo "$PWD" > /tmp/.lf-cwd
  lf -remote "send quit"
}}
map <c-g> quit-to-cwd

# trash as in the trash command
# as indicated by `$ `
map D $ trash "$f"

